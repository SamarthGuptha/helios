[package]
name = "helios-proto"
version = "0.1.0"
edition = "2021"

[lib]
name = "helios_proto"
path = "src/lib.rs"

[dependencies]
tonic.workspace = true
prost.workspace = true

[build-dependencies]
tonic-build = "0.10"
protoc-bin-vendored = "3.0.0"